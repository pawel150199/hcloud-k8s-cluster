{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"hcloud-k8s-cluster \u2014 Documentation","text":"<p>Terraform module that provisions a self-managed k3s Kubernetes cluster on Hetzner Cloud: one (or more) master nodes and a configurable number of worker nodes, all joined over a private Hetzner network.</p> <p>This folder contains the full documentation for the module. Every page renders Mermaid diagrams natively on GitHub and can also be exported to PDF (see Building the PDFs).</p>"},{"location":"#table-of-contents","title":"Table of contents","text":"# Document What's inside 1 Overview What the module does, goals, and scope 2 Architecture Diagrams: components, network topology, bootstrap sequence 3 Getting started Prerequisites, providers, remote state, first apply 4 Usage example A complete, copy-pasteable example 5 Inputs &amp; outputs Full variable and output reference 6 Operations kubeconfig, day-2 tasks, scaling, teardown"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>module \"hcloud_kubernetes_cluster\" {\n  source = \"github.com/pawel-polski/hcloud-k8s-cluster\"\n\n  master_nodes_number = 1\n  worker_nodes_number = 2\n\n  ssh_keys                 = [\"my-hcloud-ssh-key\"]\n  master_nodes_ssh_pub_key = file(\"~/.ssh/master.pub\")\n  worker_nodes_ssh_pub_key = file(\"~/.ssh/worker.pub\")\n  worker_nodes_ssh_priv_key = file(\"~/.ssh/worker\")\n}\n</code></pre> <p>See the usage example for the full configuration, and getting started for the provider and backend setup that must accompany it.</p>"},{"location":"#building-the-pdfs","title":"Building the PDFs","text":"<p>The docs use <code>md-to-pdf</code> (a headless-Chromium Markdown\u2192PDF converter) together with a small config that injects Mermaid so diagrams render as vector graphics.</p>"},{"location":"#prerequisites","title":"Prerequisites","text":"<ul> <li>Node.js \u2265 16</li> <li><code>md-to-pdf</code> \u2014 install once: <code>npm install -g md-to-pdf</code></li> <li>An internet connection at build time (Mermaid is loaded from a CDN)</li> </ul>"},{"location":"#build","title":"Build","text":"<pre><code>cd docs\n\n# Build every page + a single combined PDF into docs/pdf/\nmake            # or: ./build-pdf.sh\n\n# Build just one page\nmake 02-architecture.pdf\n\n# Remove generated PDFs\nmake clean\n</code></pre> <p>Output lands in <code>docs/pdf/</code>:</p> <ul> <li><code>01-overview.pdf</code> \u2026 <code>06-operations.pdf</code> \u2014 one PDF per page</li> <li><code>hcloud-k8s-cluster-documentation.pdf</code> \u2014 the complete manual, combined</li> </ul> <p>Note. If you don't have <code>md-to-pdf</code> and don't want a global install, the build script falls back to <code>npx md-to-pdf</code>, which downloads it on first run.</p>"},{"location":"#diagram-tooling","title":"Diagram tooling","text":"<p>Diagrams are written as fenced <code>```mermaid</code> code blocks directly inside the Markdown. This means:</p> <ul> <li>On GitHub / GitLab / VS Code they render automatically \u2014 no tooling.</li> <li>In the PDF they are rendered by Mermaid via <code>docs/pdf.config.js</code>.</li> </ul> <p>No binary image files are committed, so the diagrams stay diffable and reviewable in pull requests.</p>"},{"location":"01-overview/","title":"1. Overview","text":""},{"location":"01-overview/#what-this-module-does","title":"What this module does","text":"<p><code>hcloud-k8s-cluster</code> is a Terraform module that stands up a self-managed Kubernetes cluster on Hetzner Cloud using k3s as the Kubernetes distribution.</p> <p>On <code>terraform apply</code> it creates:</p> <ul> <li>a private Hetzner network and a subnet that every node attaches to;</li> <li>one or more master (server) nodes that bootstrap a k3s control plane;</li> <li>a configurable number of worker (agent) nodes that automatically join the   control plane over the private network.</li> </ul> <p>Nodes are provisioned entirely through cloud-init (<code>user_data</code>): k3s is installed and configured on first boot, so no separate configuration-management step or manual SSH is required for the cluster to come up.</p>"},{"location":"01-overview/#design-goals","title":"Design goals","text":"<p>These goals come from the project's top-level <code>README.md</code>:</p> <ol> <li>Infrastructure as code \u2014 the whole cluster is described in Terraform.</li> <li>Automated Kubernetes setup \u2014 the cluster bootstraps itself with no manual    steps after <code>apply</code>.</li> <li>At least one master and one worker \u2014 a minimal but real multi-node    topology.</li> <li>GitOps-ready \u2014 workloads are intended to be delivered to the cluster via    GitOps (e.g. Argo CD / Flux) rather than <code>kubectl apply</code> by hand.</li> <li>Observability \u2014 the cluster is intended to host a full monitoring stack.</li> <li>Scalable \u2014 the number of worker (and master) nodes is parameterised.</li> </ol>"},{"location":"01-overview/#why-k3s","title":"Why k3s","text":"<p>k3s is a lightweight, CNCF-certified Kubernetes distribution that installs from a single script and runs well on small ARM/x86 Hetzner instances (the default node type is <code>cax11</code>, a shared-vCPU ARM64 machine). The module deliberately disables components that don't fit this environment:</p> Disabled Why <code>traefik</code> Leave ingress choice to the user / GitOps stack <code>cloud-controller</code> (built-in) Replaced by an external cloud controller; nodes run with <code>--kubelet-arg cloud-provider=external</code> <p>The module is a starting point. The project README notes it may later be extended to bootstrap Kubernetes with other tools (e.g. <code>kubeadm</code>).</p>"},{"location":"01-overview/#scope","title":"Scope","text":"<p>In scope (what the module provisions):</p> <ul> <li>Hetzner private network + subnet</li> <li>Master node(s) with a bootstrapped k3s server</li> <li>Worker node(s) that join the server automatically</li> </ul> <p>Out of scope (you provide these):</p> <ul> <li>The Hetzner Cloud project, API token, and SSH keys</li> <li>Terraform remote state storage (the project uses AWS S3 \u2014 Hetzner has no   native state backend)</li> <li>An external cloud-controller-manager, CNI extras, ingress, storage classes</li> <li>GitOps controllers and the monitoring stack (deployed into the cluster after   it exists)</li> </ul>"},{"location":"01-overview/#repository-layout","title":"Repository layout","text":"<pre><code>hcloud-k8s-cluster/\n\u251c\u2500\u2500 network.tf          # hcloud_network + subnet\n\u251c\u2500\u2500 master-node.tf      # hcloud_server master node(s) + k3s server cloud-init\n\u251c\u2500\u2500 worker-node.tf      # hcloud_server worker node(s) + k3s agent cloud-init\n\u251c\u2500\u2500 variables.tf        # input variables\n\u251c\u2500\u2500 outputs.tf          # module outputs\n\u251c\u2500\u2500 examples/\n\u2502   \u2514\u2500\u2500 basic/          # runnable usage example\n\u2514\u2500\u2500 docs/               # this documentation\n</code></pre> <p>Continue to Architecture \u00bb</p>"},{"location":"02-architecture/","title":"2. Architecture","text":"<p>This page describes the architecture of the cluster the module builds, using Mermaid diagrams. They render natively on GitHub and are exported as vector graphics in the PDF build.</p>"},{"location":"02-architecture/#21-component-overview","title":"2.1 Component overview","text":"<p>At a high level the module creates a private network, a subnet, and a set of servers. Terraform state is kept in AWS S3 (Hetzner has no native state backend).</p> <pre><code>flowchart TB\n  subgraph AWS[\"AWS\"]\n    S3[(\"S3 bucket&lt;br/&gt;Terraform state\")]\n  end\n\n  Dev[\"Operator&lt;br/&gt;terraform apply\"] --&gt;|reads/writes state| S3\n  Dev --&gt;|hcloud API| HET\n\n  subgraph HET[\"Hetzner Cloud project\"]\n    direction TB\n    NET[\"hcloud_network&lt;br/&gt;10.0.0.0/16\"]\n    SUB[\"hcloud_network_subnet&lt;br/&gt;10.0.1.0/24 \u00b7 eu-central\"]\n    NET --- SUB\n\n    subgraph CP[\"Control plane\"]\n      M0[\"master-node-0&lt;br/&gt;k3s server&lt;br/&gt;10.0.1.1\"]\n    end\n\n    subgraph WK[\"Workers\"]\n      W0[\"worker-node-0&lt;br/&gt;k3s agent\"]\n      W1[\"worker-node-1&lt;br/&gt;k3s agent\"]\n    end\n\n    SUB --- M0\n    SUB --- W0\n    SUB --- W1\n    W0 -.join :6443.-&gt; M0\n    W1 -.join :6443.-&gt; M0\n  end\n\n  classDef master fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;\n  classDef worker fill:#e3f2fd,stroke:#1565c0,color:#0d47a1;\n  class M0 master;\n  class W0,W1 worker;\n</code></pre> <p>Key points</p> <ul> <li>Every node has an interface on the private subnet (<code>10.0.1.0/24</code>). The   master pins itself to <code>10.0.1.1</code> (<code>master_node_ip</code>); workers get automatic   private IPs.</li> <li>Nodes may also have public IPv4/IPv6 (toggled by <code>node_enable_ipv4</code> /   <code>node_enable_ipv6</code>) \u2014 used for outbound package downloads and, on the master,   for fetching the kubeconfig.</li> <li>Workers reach the control plane at <code>https://10.0.1.1:6443</code> over the private   network.</li> </ul>"},{"location":"02-architecture/#22-network-topology","title":"2.2 Network topology","text":"<pre><code>flowchart LR\n  subgraph PUB[\"Public internet\"]\n    INET((\"Internet\"))\n  end\n\n  subgraph HNET[\"hcloud_network 10.0.0.0/16\"]\n    subgraph SUBNET[\"subnet 10.0.1.0/24\"]\n      M[\"master-node-0&lt;br/&gt;priv 10.0.1.1&lt;br/&gt;pub IPv4/IPv6\"]\n      W0[\"worker-node-0&lt;br/&gt;priv 10.0.1.x\"]\n      W1[\"worker-node-1&lt;br/&gt;priv 10.0.1.x\"]\n    end\n  end\n\n  INET &lt;--&gt;|apt, get.k3s.io| M\n  INET &lt;--&gt;|apt, get.k3s.io| W0\n  INET &lt;--&gt;|apt, get.k3s.io| W1\n  M &lt;===&gt;|k8s API 6443 / SSH 22&lt;br/&gt;private| W0\n  M &lt;===&gt;|k8s API 6443 / SSH 22&lt;br/&gt;private| W1\n</code></pre> Segment CIDR Notes Network <code>10.0.0.0/16</code> <code>private_network_ip_range</code> Subnet <code>10.0.1.0/24</code> <code>eu-central</code>, type <code>cloud</code> Master private IP <code>10.0.1.1</code> <code>master_node_ip</code> (fixed) Worker private IPs <code>10.0.1.0/24</code> pool Assigned by Hetzner"},{"location":"02-architecture/#23-terraform-resource-graph","title":"2.3 Terraform resource graph","text":"<p>What Terraform actually manages, and the dependencies between resources:</p> <pre><code>flowchart TD\n  V[\"input variables\"] --&gt; NET[\"hcloud_network&lt;br/&gt;private_network\"]\n  NET --&gt; SUB[\"hcloud_network_subnet&lt;br/&gt;private_network_subnet\"]\n  SUB --&gt; M[\"hcloud_server&lt;br/&gt;master_nodes[count]\"]\n  SUB --&gt; W[\"hcloud_server&lt;br/&gt;worker_nodes[count]\"]\n  M --&gt; W\n  NET --&gt; O1[\"output:&lt;br/&gt;kubernetes_network_ip_range\"]\n  M --&gt; O2[\"output:&lt;br/&gt;master_node_ip\"]\n\n  classDef res fill:#fff3e0,stroke:#e65100,color:#bf360c;\n  classDef out fill:#f3e5f5,stroke:#6a1b9a,color:#4a148c;\n  class NET,SUB,M,W res;\n  class O1,O2 out;\n</code></pre> <p><code>depends_on</code> wires the subnet before the servers, and the workers after the master, so the control plane exists before any agent tries to join.</p>"},{"location":"02-architecture/#24-bootstrap-sequence","title":"2.4 Bootstrap sequence","text":"<p>The most important flow: how the cluster assembles itself from <code>apply</code> to a Ready worker, driven entirely by cloud-init.</p> <pre><code>sequenceDiagram\n  autonumber\n  participant TF as Terraform\n  participant HC as Hetzner API\n  participant M as master-node-0\n  participant W as worker-node-N\n\n  TF-&gt;&gt;HC: create network and subnet\n  TF-&gt;&gt;HC: create master server with user_data\n  TF-&gt;&gt;HC: create worker servers with user_data\n\n  Note over M: first boot (cloud-init)\n  M-&gt;&gt;M: apt-get update and install curl\n  M-&gt;&gt;M: install k3s server&lt;br/&gt;disable traefik and cloud-controller\n  M-&gt;&gt;M: make kubeconfig and node-token&lt;br/&gt;readable by the cluster user\n  Note over M: control plane Ready at port 6443\n\n  Note over W: first boot (cloud-init)\n  W-&gt;&gt;M: poll https 10.0.1.1:6443 until ready\n  M--&gt;&gt;W: API server responds\n  W-&gt;&gt;M: ssh to master and read node-token\n  M--&gt;&gt;W: k3s join token\n  W-&gt;&gt;W: install k3s agent&lt;br/&gt;joined to 10.0.1.1:6443\n  W-&gt;&gt;M: register node over private network\n  Note over M,W: worker joins and node becomes Ready\n</code></pre>"},{"location":"02-architecture/#what-each-role-runs","title":"What each role runs","text":"<p>Master (<code>master-node.tf</code>):</p> <pre><code>curl https://get.k3s.io | \\\n  INSTALL_K3S_EXEC=\"--disable traefik --disable-cloud-controller \\\n    --kubelet-arg cloud-provider=external\" sh -\n# then make kubeconfig + node-token readable by the \"cluster\" user\n</code></pre> <p>Worker (<code>worker-node.tf</code>):</p> <pre><code># 1. wait for the API server\nuntil curl -k https://10.0.1.1:6443; do sleep 5; done\n# 2. pull the join token from the master over SSH\nREMOTE_TOKEN=$(ssh cluster@10.0.1.1 sudo cat /var/lib/rancher/k3s/server/node-token)\n# 3. install and join\ncurl -sfL https://get.k3s.io | \\\n  K3S_URL=https://10.0.1.1:6443 K3S_TOKEN=$REMOTE_TOKEN \\\n  INSTALL_K3S_EXEC=\"--kubelet-arg cloud-provider=external\" sh -\n</code></pre> <p>Trust model. Workers authenticate to the master over SSH (using an injected private key) to read the k3s node-token, then use that token to join the API server. This is why the module takes both a worker public and private SSH key.</p> <p>Continue to Getting started \u00bb</p>"},{"location":"03-getting-started/","title":"3. Getting started","text":"<p>This page walks through everything needed to go from an empty Hetzner project to a running cluster.</p>"},{"location":"03-getting-started/#31-prerequisites","title":"3.1 Prerequisites","text":"Tool / resource Purpose Terraform \u2265 1.3 Runs the module Hetzner Cloud project + API token Target infrastructure An SSH key uploaded to Hetzner Placed on the nodes at creation (<code>ssh_keys</code>) A key pair for the <code>cluster</code> user Injected via cloud-init for the k3s join flow AWS S3 bucket (recommended) Remote Terraform state \u2014 Hetzner has no native backend"},{"location":"03-getting-started/#create-the-hetzner-api-token","title":"Create the Hetzner API token","text":"<p>In the Hetzner Cloud Console: Security \u2192 API Tokens \u2192 Generate API Token (Read &amp; Write). Export it so the provider can read it:</p> <pre><code>export HCLOUD_TOKEN=\"xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx\"\n</code></pre>"},{"location":"03-getting-started/#ssh-keys","title":"SSH keys","text":"<p>Two distinct concerns:</p> <ol> <li><code>ssh_keys</code> \u2014 the names of SSH keys already uploaded to your Hetzner    project. Hetzner installs these on the nodes' default <code>root</code> user.</li> <li><code>master_nodes_ssh_pub_key</code> / <code>worker_nodes_ssh_pub_key</code> /    <code>worker_nodes_ssh_priv_key</code> \u2014 key material injected via cloud-init for the    <code>cluster</code> user. The worker's private key lets it SSH into the master to    read the k3s join token during bootstrap (see    Architecture \u00a72.4).</li> </ol> <pre><code>ssh-keygen -t rsa -b 4096 -f ./master -N \"\"   # master.pub\nssh-keygen -t rsa -b 4096 -f ./worker -N \"\"   # worker.pub / worker\n</code></pre>"},{"location":"03-getting-started/#32-provider-and-backend","title":"3.2 Provider and backend","text":"<p>The module itself declares only resources \u2014 you configure the provider and backend in your root module. A minimal root setup:</p> <pre><code>terraform {\n  required_version = \"&gt;= 1.3\"\n\n  required_providers {\n    hcloud = {\n      source  = \"hetznercloud/hcloud\"\n      version = \"~&gt; 1.52\"\n    }\n  }\n\n  # Remote state in AWS S3 (Hetzner has no native state backend).\n  backend \"s3\" {\n    bucket = \"my-terraform-state\"\n    key    = \"hcloud-k8s-cluster/terraform.tfstate\"\n    region = \"eu-central-1\"\n  }\n}\n\nvariable \"hcloud_token\" {\n  type      = string\n  sensitive = true\n}\n\nprovider \"hcloud\" {\n  token = var.hcloud_token   # or omit and rely on HCLOUD_TOKEN\n}\n</code></pre> <p>The <code>examples/basic</code> directory ships a <code>versions.tf</code> you can use as a template.</p>"},{"location":"03-getting-started/#33-first-apply","title":"3.3 First apply","text":"<pre><code># 1. Initialise providers and backend\nterraform init\n\n# 2. Review the plan\nterraform plan\n\n# 3. Create the cluster\nterraform apply\n</code></pre> <p><code>apply</code> returns once the servers exist. The k3s bootstrap continues in the background via cloud-init \u2014 allow a few minutes before the API server and workers are Ready. Track progress by SSHing into the master:</p> <pre><code>ssh cluster@&lt;master_public_ip&gt;\nsudo journalctl -u k3s -f          # control-plane logs\nsudo kubectl get nodes -o wide     # watch workers join\n</code></pre>"},{"location":"03-getting-started/#34-fetch-the-kubeconfig","title":"3.4 Fetch the kubeconfig","text":"<p>Copy the kubeconfig off the master and point <code>kubectl</code> at it:</p> <pre><code>scp -i ./master cluster@&lt;master_public_ip&gt;:/etc/rancher/k3s/k3s.yaml ~/.kube/config\n\n# The kubeconfig points at 127.0.0.1 \u2014 repoint it at the master's IP:\nsed -i '' \"s/127.0.0.1/&lt;master_public_ip&gt;/\" ~/.kube/config\n\nkubectl get nodes\n</code></pre> <p>More day-2 operations are covered in Operations \u00bb.</p>"},{"location":"04-usage-example/","title":"4. Usage example","text":"<p>A complete, runnable example lives in <code>examples/basic</code>. This page reproduces and explains it.</p>"},{"location":"04-usage-example/#41-the-example","title":"4.1 The example","text":"<pre><code># examples/basic/main.tf\nmodule \"hcloud_kubernetes_cluster\" {\n  source = \"../../\"\n\n  # Cluster size\n  worker_nodes_number = 2\n  master_nodes_number = 1\n\n  # Node hardware / image\n  node_image    = \"ubuntu-24.04\"\n  node_type     = \"cax11\"        # ARM64, shared vCPU\n  node_location = \"fsn1\"         # Falkenstein\n\n  node_enable_ipv4 = true\n  node_enable_ipv6 = true\n\n  # SSH: Hetzner-managed key names + cloud-init key material\n  ssh_keys = [\"your_ssh_key\"]\n\n  master_nodes_ssh_pub_key  = \"master_ssh_pub_key\"\n  worker_nodes_ssh_priv_key = \"worker_ssh_priv_key\"\n  worker_nodes_ssh_pub_key  = \"worker_ssh_pub_key\"\n\n  # Networking\n  private_network_ip_range        = \"10.0.0.0/16\"\n  private_network_zone            = \"eu-central\"\n  private_network_type            = \"cloud\"\n  private_network_subnet_ip_range = \"10.0.1.0/24\"\n  master_node_ip                  = \"10.0.1.1\"\n}\n</code></pre> <pre><code># examples/basic/outputs.tf\noutput \"master_node_ip\" {\n  description = \"Master Node IP Address\"\n  value       = module.hcloud_kubernetes_cluster.master_node_ip\n}\n</code></pre> <pre><code># examples/basic/versions.tf\nterraform {\n  required_providers {\n    hcloud = {\n      source  = \"hetznercloud/hcloud\"\n      version = \"1.52.0\"\n    }\n  }\n}\n\nvariable \"hcloud_token\" {\n  sensitive = true\n}\n\nprovider \"hcloud\" {\n  token = var.hcloud_token\n}\n</code></pre>"},{"location":"04-usage-example/#42-a-more-realistic-root-module","title":"4.2 A more realistic root module","text":"<p>Rather than hardcoding secrets, load real key material from files and the token from the environment:</p> <pre><code>module \"hcloud_kubernetes_cluster\" {\n  source = \"github.com/pawel-polski/hcloud-k8s-cluster\"\n\n  master_nodes_number = 1\n  worker_nodes_number = 3\n\n  node_type     = \"cax21\"       # bump size for real workloads\n  node_location = \"nbg1\"        # Nuremberg\n\n  ssh_keys = [\"ops-laptop\"]     # a key name that exists in your hcloud project\n\n  master_nodes_ssh_pub_key  = trimspace(file(\"~/.ssh/master.pub\"))\n  worker_nodes_ssh_pub_key  = trimspace(file(\"~/.ssh/worker.pub\"))\n  worker_nodes_ssh_priv_key = file(\"~/.ssh/worker\")\n}\n\noutput \"master_ip\" {\n  value = module.hcloud_kubernetes_cluster.master_node_ip\n}\n\noutput \"network_cidr\" {\n  value = module.hcloud_kubernetes_cluster.kubernetes_network_ip_range\n}\n</code></pre>"},{"location":"04-usage-example/#43-run-it","title":"4.3 Run it","text":"<pre><code>cd examples/basic\n\nexport HCLOUD_TOKEN=\"\u2026\"\nexport TF_VAR_hcloud_token=\"$HCLOUD_TOKEN\"\n\nterraform init\nterraform apply\n\n# after cloud-init finishes on the master:\nterraform output master_node_ip\n</code></pre>"},{"location":"04-usage-example/#44-choosing-values","title":"4.4 Choosing values","text":"Setting Common choices Notes <code>node_type</code> <code>cax11</code>, <code>cax21</code>, <code>cx22</code>, <code>cpx31</code> <code>cax*</code> are ARM64; <code>cx*</code>/<code>cpx*</code> are x86 <code>node_location</code> <code>fsn1</code>, <code>nbg1</code>, <code>hel1</code>, <code>ash</code>, <code>hil</code> Keep nodes in one location for low-latency private networking <code>node_image</code> <code>ubuntu-24.04</code>, <code>debian-12</code> cloud-init assumes an <code>apt</code>-based image <code>worker_nodes_number</code> <code>1</code>\u2013<code>N</code> Scale by changing this and re-applying <code>master_nodes_number</code> <code>1</code> Single control plane; <code>master_node_ip</code> pins the master, so keep this at <code>1</code> <p>Continue to Inputs &amp; outputs \u00bb</p>"},{"location":"05-inputs-and-outputs/","title":"5. Inputs &amp; outputs","text":"<p>Full reference for the module's variables (<code>variables.tf</code>) and outputs (<code>outputs.tf</code>).</p>"},{"location":"05-inputs-and-outputs/#51-inputs","title":"5.1 Inputs","text":""},{"location":"05-inputs-and-outputs/#cluster-sizing","title":"Cluster sizing","text":"Name Type Default Description <code>master_nodes_number</code> <code>number</code> <code>1</code> Number of master (k3s server) nodes <code>worker_nodes_number</code> <code>number</code> <code>2</code> Number of worker (k3s agent) nodes"},{"location":"05-inputs-and-outputs/#node-configuration","title":"Node configuration","text":"Name Type Default Description <code>node_image</code> <code>string</code> <code>\"ubuntu-24.04\"</code> OS image for every node (must be <code>apt</code>-based for cloud-init) <code>node_type</code> <code>string</code> <code>\"cax11\"</code> Hetzner server type (e.g. <code>cax11</code>, <code>cx22</code>) <code>node_location</code> <code>string</code> <code>\"fsn1\"</code> Hetzner location (<code>fsn1</code>, <code>nbg1</code>, <code>hel1</code>, \u2026) <code>node_enable_ipv4</code> <code>bool</code> <code>true</code> Attach a public IPv4 to nodes <code>node_enable_ipv6</code> <code>bool</code> <code>true</code> Attach a public IPv6 to nodes <code>master_node_ip</code> <code>string</code> <code>\"10.0.1.1\"</code> Fixed private IP assigned to the master node"},{"location":"05-inputs-and-outputs/#networking","title":"Networking","text":"Name Type Default Description <code>private_network_ip_range</code> <code>string</code> <code>\"10.0.0.0/16\"</code> CIDR of the private <code>hcloud_network</code> <code>private_network_subnet_ip_range</code> <code>string</code> <code>\"10.0.1.0/24\"</code> CIDR of the subnet\u00b9 <code>private_network_zone</code> <code>string</code> <code>\"eu-central\"</code> Network zone of the subnet\u00b9 <code>private_network_type</code> <code>string</code> <code>\"cloud\"</code> Subnet type\u00b9"},{"location":"05-inputs-and-outputs/#ssh-keys","title":"SSH keys","text":"Name Type Default Description <code>ssh_keys</code> <code>list(string)</code> <code>null</code> Names of SSH keys already present in the Hetzner project, installed on nodes' <code>root</code> user <code>master_nodes_ssh_pub_key</code> <code>string</code> <code>null</code> Public key added to the master's <code>cluster</code> user via cloud-init\u00b2 <code>worker_nodes_ssh_pub_key</code> <code>string</code> <code>null</code> Public key added to the worker's <code>cluster</code> user via cloud-init\u00b2 <code>worker_nodes_ssh_priv_key</code> <code>string</code> <code>null</code> Private key written to workers so they can SSH into the master to read the k3s join token <p>\u00b9 Currently informational. <code>network.tf</code> hardcodes the subnet's <code>type</code>, <code>network_zone</code>, and <code>ip_range</code>, so these three variables are not yet wired into the subnet resource. Set them for forward-compatibility, but changing them won't move the subnet until the module is updated.</p> <p>\u00b2 The cloud-init templates in <code>master-node.tf</code> / <code>worker-node.tf</code> reference these keys via <code>user_data</code>. Make sure the <code>user_data</code> references match the declared variable names before applying.</p>"},{"location":"05-inputs-and-outputs/#52-outputs","title":"5.2 Outputs","text":"Name Type Description <code>kubernetes_network_ip_range</code> <code>string</code> CIDR of the created private network (<code>private_network_ip_range</code>) <code>master_node_ip</code> <code>string</code> / <code>list</code> Public IPv4 address of the master node(s) <p>Example consumption from a root module:</p> <pre><code>output \"master_ip\" {\n  value = module.hcloud_kubernetes_cluster.master_node_ip\n}\n\noutput \"network_cidr\" {\n  value = module.hcloud_kubernetes_cluster.kubernetes_network_ip_range\n}\n</code></pre>"},{"location":"05-inputs-and-outputs/#53-resources-created","title":"5.3 Resources created","text":"Resource Terraform address Count Private network <code>hcloud_network.private_network</code> 1 Subnet <code>hcloud_network_subnet.private_network_subnet</code> 1 Master server(s) <code>hcloud_server.master_nodes</code> <code>master_nodes_number</code> Worker server(s) <code>hcloud_server.worker_nodes</code> <code>worker_nodes_number</code> <p>Continue to Operations \u00bb</p>"},{"location":"06-operations/","title":"6. Operations","text":"<p>Day-2 tasks for a running cluster.</p>"},{"location":"06-operations/#61-accessing-the-cluster","title":"6.1 Accessing the cluster","text":"<p>Copy the kubeconfig off the master and repoint it at the master's reachable IP:</p> <pre><code>scp -i ./master cluster@&lt;master_public_ip&gt;:/etc/rancher/k3s/k3s.yaml ~/.kube/config\nsed -i '' \"s/127.0.0.1/&lt;master_public_ip&gt;/\" ~/.kube/config   # macOS sed\nkubectl get nodes -o wide\n</code></pre> <p>For a private-only setup, tunnel to the API server instead of exposing it:</p> <pre><code>ssh -i ./master -L 6443:10.0.1.1:6443 cluster@&lt;master_public_ip&gt;\n# then use a kubeconfig pointing at https://127.0.0.1:6443\n</code></pre>"},{"location":"06-operations/#62-verifying-bootstrap","title":"6.2 Verifying bootstrap","text":"<pre><code># on the master\nsudo systemctl status k3s\nsudo journalctl -u k3s -f\n\n# on a worker\nsudo systemctl status k3s-agent\nsudo journalctl -u k3s-agent -f\n</code></pre> <p>Expected end state: <code>kubectl get nodes</code> shows the master plus <code>worker_nodes_number</code> agents in <code>Ready</code>.</p>"},{"location":"06-operations/#63-scaling","title":"6.3 Scaling","text":"<p>The cluster size is parameterised \u2014 change the count and re-apply.</p> <pre><code>flowchart LR\n  A[\"edit worker_nodes_number&lt;br/&gt;2 \u2192 4\"] --&gt; B[\"terraform plan\"]\n  B --&gt; C[\"terraform apply\"]\n  C --&gt; D[\"2 new servers boot\"]\n  D --&gt; E[\"cloud-init joins them&lt;br/&gt;to 10.0.1.1:6443\"]\n  E --&gt; F[\"kubectl get nodes&lt;br/&gt;shows 4 workers\"]\n</code></pre> <pre><code># scale workers up (or down)\nterraform apply -var 'worker_nodes_number=4'\n</code></pre> <p>Scaling down removes the highest-indexed servers. Drain them first so workloads reschedule cleanly:</p> <pre><code>kubectl drain worker-node-3 --ignore-daemonsets --delete-emptydir-data\nterraform apply -var 'worker_nodes_number=3'\nkubectl delete node worker-node-3     # remove the stale Node object\n</code></pre>"},{"location":"06-operations/#64-what-to-deploy-next","title":"6.4 What to deploy next","text":"<p>The module deliberately ships a bare cluster (Traefik and the built-in cloud controller are disabled). Typical next steps, ideally via GitOps:</p> Concern Options Cloud controller hcloud-cloud-controller-manager (nodes already run <code>cloud-provider=external</code>) CNI / networking k3s ships Flannel by default; swap for Cilium/Calico if needed Ingress ingress-nginx, Traefik (re-enabled), or Gateway API Storage hcloud-csi-driver for Hetzner Volumes GitOps Argo CD or Flux Monitoring kube-prometheus-stack (Prometheus + Grafana + Alertmanager)"},{"location":"06-operations/#65-teardown","title":"6.5 Teardown","text":"<pre><code>terraform destroy\n</code></pre> <p>This removes the servers, subnet, and network. State lives in S3 and is unaffected \u2014 delete the state object separately if you're retiring the workspace.</p> <p>Before destroying: back up anything you need (PersistentVolumes, etc.). Hetzner Volumes and Load Balancers created inside the cluster by controllers are not managed by this module and may need separate cleanup.</p> <p>Back to the documentation index.</p>"}]}